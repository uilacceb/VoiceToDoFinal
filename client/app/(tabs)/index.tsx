import TodoList from "../../components/TodoList";
import { View } from "react-native";

export default function Index() {
  return (
    <View style={{ flex: 1 }}>
      <TodoList />
    </View>
  );
}
